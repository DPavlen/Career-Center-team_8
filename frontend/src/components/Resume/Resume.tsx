import CandidateInfo from '../CandidateInfo/CandidateInfo';
import './Resume.scss';

function Resume() {
  return (
    <main className="candidate">
      <CandidateInfo />
    </main>
  );
}

export default Resume;
